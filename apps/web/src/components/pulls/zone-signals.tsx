"use client";

import type { SignalChip } from "@/lib/github/types";

export interface ZoneSignalsProps {
  chips: SignalChip[];
  loading: boolean;
  error: boolean;
}

export function ZoneSignals(_props: ZoneSignalsProps) {
  return null;
}
